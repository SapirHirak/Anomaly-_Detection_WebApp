function Table() {

    /*function handleEnter(event) {
        if (event.code === 'Enter') {
            addItem(event.target.value);
        }
    }*/

    return (

        <div>
            Im Table
        </div>
    )
}

export default Table;