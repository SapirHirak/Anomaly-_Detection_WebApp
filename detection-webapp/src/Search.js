import './App.css';

function Search() {

    /*function handleEnter(event) {
        if (event.code === 'Enter') {
            addItem(event.target.value);
        }
    }*/

    return (

        <div className="searchDiv">
            Im Search
            <input />
        </div>
    )
}

export default Search;