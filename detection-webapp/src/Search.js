function Search() {

    /*function handleEnter(event) {
        if (event.code === 'Enter') {
            addItem(event.target.value);
        }
    }*/

    return (

        <div>
            Im Search
        </div>
    )
}

export default Search;