function Graph() {

    /*function handleEnter(event) {
        if (event.code === 'Enter') {
            addItem(event.target.value);
        }
    }*/

    return (

        <div>
            Im Graph
        </div>
    )
}

export default Graph;