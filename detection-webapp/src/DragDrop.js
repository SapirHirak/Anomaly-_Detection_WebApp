function DragDrop() {

    /*function handleEnter(event) {
        if (event.code === 'Enter') {
            addItem(event.target.value);
        }
    }*/

    return (

        <div>
            Im DragDrop
        </div>
    )
}

export default DragDrop;